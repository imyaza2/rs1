
# 🤖 ربات خبرخوان هوشمند (RSS Bot Admin)

این پروژه برای استقرار خودکار روی **Cloudflare Pages** از طریق **GitHub** طراحی شده است.

---

## 🚀 راهنمای نصب اتوماتیک (گام به گام)

### مرحله ۱: آپلود در GitHub
1. یک مخزن (Repository) جدید در گیتهاب بسازید.
2. کدهای پروژه را در آن Push کنید.

### مرحله ۲: اتصال به Cloudflare Pages
1. وارد پنل [Cloudflare Dashboard](https://dash.cloudflare.com) شوید.
2. به بخش **Workers & Pages** بروید.
3. روی دکمه **Create Application** و سپس تب **Pages** کلیک کنید.
4. دکمه **Connect to Git** را بزنید و مخزن گیتهاب خود را انتخاب کنید.

### مرحله ۳: تنظیمات Build (مهم)
در صفحه تنظیمات Deploy موارد زیر را چک کنید:
*   **Framework Preset**: `Vite`
*   **Build Command**: `npm run build`
*   **Build Output Directory**: `dist`

### مرحله ۴: تنظیم متغیرهای محیطی (Environment Variables)
در همان صفحه ساخت، یا بعداً در بخش `Settings > Environment Variables` متغیر زیر را اضافه کنید:
*   `API_KEY`: کلید API هوش مصنوعی Gemini گوگل.

---

## 💾 مرحله ۵: اتصال دیتابیس (بسیار مهم)

برای اینکه تنظیمات ذخیره شوند، باید دیتابیس KV را متصل کنید:

1. در داشبورد کلادفلر به مسیر **Workers & Pages > KV** بروید.
2. روی **Create a Namespace** کلیک کنید و نام آن را مثلاً `RSS_DB` بگذارید.
3. حالا به پروژه Pages خود برگردید.
4. به تب **Settings** و سپس **Functions** بروید.
5. بخش **KV Namespace Bindings** را پیدا کنید.
6. روی **Add Binding** کلیک کنید:
    *   **Variable name**: `DB` (حتماً باید دقیقاً همین باشد - حروف بزرگ)
    *   **KV Namespace**: دیتابیسی که در مرحله ۲ ساختید (`RSS_DB`) را انتخاب کنید.
7. پروژه را یک بار **Redeploy** کنید (از بخش Deployments روی سه نقطه آخرین بیلد کلیک و Retry کنید).

---

## ❓ عیب‌یابی (Troubleshooting)

### خطای "KV Binding 'DB' not found"
اگر در لاگ‌ها یا هنگام ذخیره تنظیمات این خطا را دیدید، یعنی مرحله ۵ به درستی انجام نشده است.
1. مطمئن شوید در بخش Settings > Functions > KV Bindings، نام متغیر دقیقاً `DB` است.
2. حتماً بعد از اضافه کردن Binding، باید یک بار **Redeploy** کنید تا تغییرات اعمال شود.

### اجرای لوکال با دیتابیس
برای اجرای پروژه روی سیستم خودتان با دسترسی به دیتابیس شبیه‌سازی شده:
`npm run start:kv`
