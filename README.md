
# 🤖 ربات خبرخوان و پنل مدیریت هوشمند (RSS Bot Admin)

این پروژه یک پنل مدیریت وب مدرن و پیشرفته برای ربات‌های خبرخوان است که بر پایه **React** و **Cloudflare Pages** ساخته شده است. این سیستم به شما اجازه می‌دهد فیدهای RSS را مدیریت کنید، محتوا را با هوش مصنوعی (Gemini) پردازش کرده و به صورت هوشمند به کانال‌های مختلف **تلگرام** و **بله** ارسال کنید.

---

## ✨ ویژگی‌های کلیدی

*   **ذخیره‌سازی ابری (Cloudflare KV)**: تمام تنظیمات، کانال‌ها و فیدها در دیتابیس کلادفلر ذخیره می‌شوند.
*   **مدیریت کانال‌ها (نامحدود)**: تعریف بی‌نهایت کانال تلگرام و بله با قابلیت تنظیم توکن ربات اختصاصی برای هر کانال.
*   **مسیردهی هوشمند (Smart Routing)**: قابلیت ارسال اخبار، تصاویر و ویدیوهای یک فید خاص به کانال‌های جداگانه.
*   **هوش مصنوعی Gemini**: تولید خودکار کپشن فارسی جذاب برای پست‌ها.
*   **پشتیبانی از گالری**: تشخیص و ارسال آلبوم‌های عکس.
*   **صف پردازش (Queue)**: سیستم مدیریت صف برای جلوگیری از محدودیت‌های Rate Limit.

---

## 🚀 راهنمای نصب و استقرار کامل (Wrangler CLI)

برای استفاده از تمام قابلیت‌ها (شامل دیتابیس)، باید پروژه را با `wrangler` دپلوی کنید.

### 1. پیش‌نیازها
*   نصب Node.js
*   داشتن اکانت Cloudflare

### 2. نصب وابستگی‌ها
```bash
npm install
```

### 3. لاگین به کلادفلر
```bash
npx wrangler login
```

### 4. ساخت دیتابیس (KV Namespace)
برای ذخیره اطلاعات، باید یک فضای ذخیره‌سازی در کلادفلر بسازید:
```bash
npx wrangler kv:namespace create "DB"
```
**مهم:** بعد از اجرای دستور بالا، خروجی ترمینال شامل `id` را کپی کنید و در فایل `wrangler.toml` جایگزین `YOUR_KV_NAMESPACE_ID_HERE` کنید.

### 5. استقرار (Deploy)
```bash
npm run build
npx wrangler pages deploy dist
```

بعد از اتمام، آدرس سایت به شما نمایش داده می‌شود. وارد تنظیمات سایت در داشبورد کلادفلر شوید و در بخش **Environment Variables**، مقدار `API_KEY` (مربوط به Gemini) را وارد کنید.

---

## ☁️ ساختار دیتابیس
این برنامه از کلیدهای زیر در KV استفاده می‌کند:
*   `channels`: لیست کانال‌ها
*   `feeds`: لیست فیدهای RSS
*   `settings`: تنظیمات کلی (توکن‌ها، ساعات خاموشی)

---

## ⚠️ نکته مهم توسعه (Local Development)
برای اجرای لوکال با قابلیت‌های KV، از دستور زیر استفاده کنید:
```bash
npx wrangler pages dev dist
```
(ابتدا باید `npm run build` را اجرا کرده باشید).
