
# 🤖 ربات خبرخوان و پنل مدیریت هوشمند (RSS Bot Admin)

این پروژه یک پنل مدیریت وب مدرن و پیشرفته برای ربات‌های خبرخوان است که بر پایه **React** و **Cloudflare Pages** ساخته شده است. این سیستم به شما اجازه می‌دهد فیدهای RSS را مدیریت کنید، محتوا را با هوش مصنوعی (Gemini) پردازش کرده و به صورت هوشمند به کانال‌های مختلف **تلگرام** و **بله** ارسال کنید.

---

## ✨ ویژگی‌های کلیدی

*   **مدیریت کانال‌ها (نامحدود)**: تعریف بی‌نهایت کانال تلگرام و بله با قابلیت تنظیم توکن ربات اختصاصی برای هر کانال.
*   **مسیردهی هوشمند (Smart Routing)**: قابلیت ارسال اخبار، تصاویر و ویدیوهای یک فید خاص به کانال‌های جداگانه (مثلاً ارسال ویدیوهای ورزشی به کانال A و اخبار متنی به کانال B).
*   **هوش مصنوعی Gemini**: تولید خودکار کپشن فارسی جذاب برای پست‌ها و اصلاح متن.
*   **پشتیبانی از گالری**: تشخیص و ارسال آلبوم‌های عکس (چندتایی) با رعایت محدودیت‌های تلگرام.
*   **صف پردازش (Queue)**: سیستم مدیریت صف برای جلوگیری از محدودیت‌های Rate Limit تلگرام و بله.
*   **فیلترینگ کیفیت**: الگوریتم شبیه‌سازی شده برای انتخاب بهترین کیفیت تصاویر و ویدیوها.
*   **رابط کاربری مدرن**: طراحی شده با Tailwind CSS و فونت وزیرمتن، کاملاً ریسپانسیو.

---

## 🚀 پیش‌نیازها

*   **Node.js** (نسخه 18 یا بالاتر)
*   **حساب کاربری Cloudflare** (برای استقرار)
*   **API Key گوگل (Gemini)** (برای قابلیت‌های هوش مصنوعی)
*   **توکن ربات تلگرام/بله** (از BotFather یا بازوی بله)

---

## 🛠 نصب و اجرا روی سیستم شخصی (Local)

1.  **دریافت پروژه**:
    ```bash
    git clone https://github.com/your-username/rss-bot-admin.git
    cd rss-bot-admin
    ```

2.  **نصب وابستگی‌ها**:
    ```bash
    npm install
    ```

3.  **تنظیم کلید API (اختیاری در لوکال)**:
    یک فایل `.env` بسازید و کلید جمنای را در آن قرار دهید (یا مستقیماً در کد تست کنید):
    ```env
    API_KEY=your_google_gemini_api_key
    ```

4.  **اجرا**:
    ```bash
    npm run dev
    ```
    حالا پروژه در آدرس `http://localhost:5173` در دسترس است.

---

## ☁️ راهنمای استقرار در Cloudflare Pages

این پروژه برای اجرا روی زیرساخت Cloudflare بهینه شده است.

1.  کد پروژه را در یک مخزن **GitHub** پوش (Push) کنید.
2.  به داشبورد **Cloudflare** بروید و وارد بخش **Compute (Workers & Pages)** شوید.
3.  روی دکمه **Create Application** و سپس **Pages** کلیک کنید و **Connect to Git** را بزنید.
4.  مخزن گیت‌هاب خود را انتخاب کنید.
5.  در بخش **Build settings** تنظیمات زیر را وارد کنید:
    *   **Framework preset**: `Vite`
    *   **Build command**: `npm run build`
    *   **Build output directory**: `dist`
6.  در بخش **Environment variables** یک متغیر جدید اضافه کنید:
    *   Variable name: `API_KEY`
    *   Value: `کلید_API_جمنای_شما`
7.  روی **Save and Deploy** کلیک کنید.

---

## ⚙️ پیکربندی و تنظیمات اولیه

پس از بالا آمدن پنل مدیریت، وارد تب **Settings** شوید:

1.  **توکن‌های پیش‌فرض**: توکن ربات تلگرام و بله اصلی خود را وارد کنید. این توکن‌ها برای کانال‌هایی استفاده می‌شوند که توکن اختصاصی ندارند.
2.  **CORS Proxy**: به دلیل اجرای کلاینت-ساید، درخواست‌ها باید از یک پراکسی عبور کنند. مقدار پیش‌فرض `https://corsproxy.io/?` است. برای امنیت بیشتر در محیط پروداکشن، پیشنهاد می‌شود یک Cloudflare Worker شخصی برای پراکسی بسازید.

### 📡 نحوه اتصال کانال‌ها و مسیردهی

1.  به تب **مدیریت کانال‌ها** بروید.
2.  کانال‌های مقصد خود را تعریف کنید (پلتفرم، آیدی کانال و در صورت نیاز توکن ربات متفاوت).
3.  به تب **مدیریت فیدها** بروید.
4.  یک فید RSS اضافه کنید.
5.  روی دکمه **مسیردهی (Routing)** کلیک کنید.
6.  مشخص کنید که محتوای "عمومی"، "تصاویر" و "ویدیو"های این فید به کدام کانال‌ها ارسال شوند.

---

## 📦 ساختار پروژه

*   `index.tsx`: هسته اصلی برنامه شامل تمام لاجیک‌ها (UI، صف، فید منیجر).
*   `vite.config.ts`: تنظیمات بیلد برای کلادفلر.
*   `package.json`: اسکریپت‌ها و وابستگی‌ها.

---

## ⚠️ نکته مهم درباره CORS

از آنجایی که این یک اپلیکیشن سمت کاربر (Client-Side) است که مستقیماً به API تلگرام و بله درخواست می‌فرستد، مرورگرها به دلیل سیاست‌های امنیتی (CORS) جلوی درخواست مستقیم را می‌گیرند.
به همین دلیل در تنظیمات پیش‌فرض از سرویس `corsproxy.io` استفاده شده است. اگر با خطای شبکه مواجه شدید، مطمئن شوید این سرویس در دسترس است یا از پراکسی شخصی خود استفاده کنید.
